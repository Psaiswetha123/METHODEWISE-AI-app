package ai.methodwise.app

import android.app.Application

class MethodWiseApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize global app configuration, push notifications, and analytics
    }
}
